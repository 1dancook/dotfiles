# Install NODE.js and NPM


echo "Installing NODE and NPM via homebrew"

# run the following command into the Terminal application to install both Node.js and npm:
brew install node


# TODO: the recommended way is to use nvm to manage the installer
# https://github.com/nvm-sh/nvm
